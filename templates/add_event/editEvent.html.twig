{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}

{# ----------------------HEADER PART---------------------------------- #}

{# TITLE PART------------------ #}

<div id="titleNav">
    <h1><a href="{{ path('app_home') }}"> Bouger à Périgueux</a></h1>
</div>

<div id="addEventContainer">
    
    <div class='addEvent'>
        <h1>Ajouter un évènement</h1>
        {{ form_start(eventForm) }}
            {{ form_row(eventForm.name) }}
            {{ form_row(eventForm.fixed) }}
            {{ form_row(eventForm.eventDate) }}
            {{ form_row(eventForm.duration) }}
            {{ form_row(eventForm.price) }}
            {{ form_row(eventForm.enterpriseName) }}
            {{ form_row(eventForm.description) }}
            {{ form_row(eventForm.locality) }}
            {{ form_row(eventForm.image) }}
            {{ form_row(eventForm.link) }}
            {{ form_row(eventForm.type) }}
        
        {{ form_end(eventForm) }}
    </div>
</div>



{{ include('footer.html') }}
{% endblock %}